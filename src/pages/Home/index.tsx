import React from 'react';
import { PageWrapper, CardList, SearchForm } from 'components';

const Home = () => (
  <PageWrapper>
    <SearchForm />
    <CardList />
  </PageWrapper>
);

export default Home;
